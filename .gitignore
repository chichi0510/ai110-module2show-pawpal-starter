# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Virtualenv
.venv/
.venv_new/

# OS / editor
.DS_Store
.vscode/
.idea/

# Secrets & runtime data (never commit)
.env

# Build artefacts (regenerated on every machine)
chroma_db/
logs/
logs/*.jsonl

# Eval reports are regenerated from `python -m eval.run_eval --all`.
# We commit *summaries* into `docs/EVAL_RESULTS.md` instead.
eval/reports/
