"""Behavioural evaluation harness for the AI layers.

Phase 1 ships `golden_qa.jsonl` (RAG correctness + safety baselines) and
`run_eval.py`. Later phases will add `planning_goals.jsonl` (Phase 2),
`bias_probes.jsonl` (Phase 3), and `red_team.jsonl` (Phase 4).
"""
